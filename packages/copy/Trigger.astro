---
import { context } from "@astro-aria/context";

type Props = {
  [x: string]: any;
};

const { ...attributes } = Astro.props;
---

<button type="button" id={`trigger:${context("id")}`} data-state="inactive" {...attributes}>
  <slot />
</button>
