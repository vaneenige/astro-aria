---
import { context } from "@astro-aria/context";

const { ...nodeProps } = Astro.props;
---

<div id={`content:${context('id')}`} aria-labelledby={`trigger:${context('id')}`} data-state={context('open')} hidden={context('open')} {...nodeProps}><slot /></div>