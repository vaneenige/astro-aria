---
import { context } from "@astro-aria/context";

const { ...nodeProps } = Astro.props;
---

<button type="button" id={`trigger:${context('uuid')}`} aria-controls={`content:${context('uuid')}`} aria-expanded={context('open')} data-state={context('open')} {...nodeProps}><slot /></button>