---
import { context } from "@astro-aria/context";

const { id = "$id", ...nodeProps } = Astro.props;
---

<button
  role="tab"
  id={`tab:${context("uuid")}:${id}`}
  aria-selected={context("value", id)}
  tabindex={context("value", id)}
  aria-controls={`tabpanel:${context("uuid")}:${id}`}
  data-state={context("value", id)}
  {...nodeProps}
><slot /></button>
